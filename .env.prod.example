# Production environment template.
# Copy to .env.prod and fill with real values.
# NEVER commit .env.prod.

# Runtime
NODE_ENV=production
PORT=3000
LAZYFLOW_APP_IMAGE=lazyflow-app:latest
LAZYFLOW_PROXY_IMAGE=lazyflow-caddy:latest

# Reverse proxy TLS
DOMAIN=your-domain.com
ACME_EMAIL=admin@your-domain.com

# Public URLs (must be HTTPS and stable)
APP_URL=https://your-domain.com
MINI_APP_URL=https://your-domain.com

# CORS allowlist for API (comma-separated, no wildcard in prod)
CORS_ALLOWED_ORIGINS=https://your-domain.com

# Telegram Bot
BOT_TOKEN=
WEBHOOK_SECRET=

# Persistence
DB_PATH=/app/data/lazyflow.db

# AI (optional but recommended in production)
OPENAI_API_KEY=
GEMINI_API_KEY=

# Google Calendar (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://your-domain.com/google/callback
GOOGLE_OAUTH_SCOPES=https://www.googleapis.com/auth/calendar.events
GOOGLE_OAUTH_STATE_SECRET=

# Secret generation example:
#   openssl rand -hex 32
